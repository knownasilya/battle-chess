<div
  class='rounded shadow-md w-full h-full flex flex-col items-stretch justify-between hover:shadow-lg
    {{if (eq @isInPlay) "border-indigo-700 border-2" "border-gray-500 border"}}'
>
  <div
    class='your-card__bg h-16 text-center flex flex-col justify-center text-white bg-orange-900'
  >
    {{@card.name}}
  </div>

  <p class='p-4'>{{@card.description}}</p>

  {{yield @card}}
</div>